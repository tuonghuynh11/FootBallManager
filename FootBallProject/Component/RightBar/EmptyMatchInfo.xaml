<UserControl x:Class="FootBallProject.Component.RightBar.EmptyMatchInfo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FootBallProject.Component.RightBar"
             mc:Ignorable="d" 
                 Background="{StaticResource SideBar}"
    FontFamily="{DynamicResource MaterialDesignFont}"
    TextElement.FontSize="13"
    TextElement.FontWeight="Regular"
    TextElement.Foreground="{DynamicResource MaterialDesignBody}"
    TextOptions.TextFormattingMode="Ideal"
    TextOptions.TextRenderingMode="Auto"
             d:DesignHeight="700" d:DesignWidth="355">
    <Grid>
        <Viewbox>
            <StackPanel
                Margin="32"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">

                <TextBlock
                    Margin="32"
                    FontSize="20"
                    Text="{Binding Guideline, FallbackValue='Vui lòng chọn xem thông tin để hiển thị'}"
                    TextAlignment="Center" />
            </StackPanel>
        </Viewbox>
    </Grid>
</UserControl>
